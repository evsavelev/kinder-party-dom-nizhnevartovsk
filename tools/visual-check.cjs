const {chromium}=require('@playwright/test');const fs=require('node:fs');
(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});for(const w of [390,1440]){const p=await b.newPage({viewport:{width:w,height:900}});await p.goto(process.env.QA_URL||'http://127.0.0.1:4174/',{waitUntil:'networkidle'});await p.addStyleTag({content:'html{scroll-behavior:auto!important}'});for(const id of ['venue','gallery','contacts']){await p.locator('#'+id).scrollIntoViewIfNeeded();await p.waitForTimeout(400);await p.screenshot({path:`.research/qa/final-${id}-${w}.png`});}console.log(w,await p.locator('#contacts').innerText());await p.close()}await b.close()})();
